# Copy this file to .env.local (for dev) or provide these variables in production
# Make sure this API key belongs to the same org/project as your Agent Builder workflow
OPENAI_API_KEY=sk-your_api_key_here

# Workflow id from Agent Builder (starts with wf_...)
NEXT_PUBLIC_CHATKIT_WORKFLOW_ID=wf_replace_with_your_workflow_id

# Optional: override the API base for ChatKit
# CHATKIT_API_BASE=https://api.openai.com
